# PLAN: Claudeのトークン回復通知アプリ (token-recovery-notifier)

作成日: 2026-07-23
関連: CONCEPT.md / processgraph `ludqoze`（Claudeのトークン回復通知アプリ）/ `rtrx6va`（アプリ制作ゴール）

## 目的（Why）

Claude Code CLIを日常的に使う開発者は、セッション（5時間ごと）や週次のレート制限に達すると、リセットされるまで作業を中断せざるを得ない。現状は「手動でタイマーを設定する」「時々ターミナルを覗いて確認する」といった非効率な対処しかできていない。本アプリは、Windows上でClaude Code CLIの使用量上限がリセットされたタイミングをOS通知で自動的に知らせることで、この待ち時間の管理コストをゼロにする。

競合調査（CONCEPT.md参照）の結果、Windows向けのこの種のツールは個人OSSの無料アプリのみでUI/サポートが手薄であり、収益化しているのはmacOS専用のSessionWatcherのみという市場の隙間がある。Claude Code専用に絞ることでスコープを小さく保ちつつ、無料＋投げ銭モデルで広く使われることを目指す。

## 技術調査結果（最重要）

### 調査方法

Windows環境（`C:\Users\chiko`配下）を実際に探索し、Claude Code CLIがローカルに保存するファイルを調査した。加えて、本セッション内に残っていた過去の同一調査（このタスクを実行する以前の試行が、実際にレート制限に達して中断した際の途中経過ログ）も参照し、Web検索で得られていた競合OSSの実装方式に関する情報も合わせて突き合わせた。

### 発見1: セッションtranscript(jsonl)にレート制限ヒットの情報が実際に記録されている（確認済み）

Claude Code CLIは、対話履歴を `%USERPROFILE%\.claude\projects\<プロジェクトパスをエンコードしたディレクトリ名>\<sessionId>.jsonl` に1行1メッセージのJSON Lines形式で保存している（サブエージェント使用時は同ディレクトリ配下 `subagents\agent-*.jsonl` にも同様の記録が残る）。

このファイルを実際に調査したところ、**セッション（5時間）のレート制限に達した際、CLIが以下のような「合成（synthetic）アシスタントメッセージ」をtranscriptに追記していることを実データで複数件確認した**：

```json
{
  "type": "assistant",
  "message": {
    "model": "<synthetic>",
    "content": [{"type": "text", "text": "You've hit your session limit · resets 11pm (Etc/GMT-9)"}]
  },
  "error": "rate_limit",
  "isApiErrorMessage": true,
  "apiErrorStatus": 429,
  "version": "2.1.215"
}
```

過去2〜3週間分・4つの異なるプロジェクトのtranscriptから、この形式のメッセージを**20件以上**実データとして確認した。文言パターンは一貫して以下の形式だった：

```
hit your session limit · resets <H(:MM)?(am|pm)> (Etc/GMT-9)
```

例: `resets 11pm`、`resets 1:30pm`、`resets 2am`、`resets 12:10pm` など。`(Etc/GMT-9)` はPOSIX慣習の符号反転オフセット表記で、ユーザーのシステムタイムゾーン（この環境ではJST=UTC+9）を表している。

### 発見2: 既存OSS競合の実装方式（同セッション内に残っていた過去のWeb調査結果より）

本タスクの実行中、以前に同種の調査を行った際のログ（レート制限により中断した過去の試行の断片）が同一マシン上に残っていた。そこに記録されていたWeb検索結果によれば、既存のOSS競合ツールは以下の方式でこの問題に対応している：

- **claude-auto-retry**: tmuxペインを5秒ごとにポーリングし、ターミナル出力中のレート制限文言をスクレイピングして解析。
- **claude-reset**: 非公開（private/undocumented）のAnthropicエンドポイントを数分おきにポーリングし、`resets_at`タイムスタンプが1時間以上先に進んだことを検知してリセットと判定。
- **unsnooze**: ntfy等のプッシュ通知でリセットを通知（データ取得方式は主にターミナル出力解析）。
- **code-notify**: 残量%が20%/10%を下回った警告、100%に戻った際のリセット通知。

これらはいずれも「CLIがすでに表示しているレート制限メッセージ」を何らかの形でパースしている点で共通しており、今回発見したtranscript(jsonl)内の合成メッセージも本質的に同じ情報源（CLI自身が生成する通知テキスト）にあたる。

### 実現可能性の評価: 実現可能（ただし限定的な留保あり）

**結論: 技術的に実現可能。** 上記の通り、Claude Code CLIはレート制限ヒット時に人間可読な「いつリセットされるか」の情報をローカルファイルに平文で記録しており、これを定期的に監視・パースすることで「上限に達したこと」と「いつリセットされるか」の両方を追加の認証情報や外部APIコールなしに取得できる。

ただし以下の留保・未確認事項がある：

1. **週次（weekly）レート制限のメッセージ形式は未確認。** 今回実データで確認できたのはすべて「session limit」（5時間ウィンドウ）のメッセージであり、週次上限に達した際の合成メッセージの文言・フィールド構成は実例を取得できていない。他ツールの説明に「5-hour/weekly usage limit」という記述があることから類似の仕組みが存在すると推測されるが、**要確認事項**として実装フェーズ（フェーズA）で追加検証する必要がある。
2. **このファイル形式・文言は非公式（undocumented）の内部実装。** Anthropicが安定APIとして保証しているものではなく、CLIのバージョンアップで形式が変わりうる（破壊的変更リスク）。実際、調査中に観測したtranscriptのversionフィールドだけでも `2.1.197`〜`2.1.217` の範囲でバージョンが変化しているが、少なくともこの範囲内ではメッセージ形式は一貫していた。
3. **タイムゾーン表記のパースには汎用的な実装が必要。** `Etc/GMT-9` のような符号反転オフセット表記を決め打ちで「+9」と仮定せず、一般的なオフセットに対応するパーサーが必要。

代替アプローチ（tmux監視、非公式エンドポイントのポーリング、`/status`コマンドの定期実行促し等）も検討したが、いずれもWindowsネイティブでの実現性・保守性・リスクの面でローカルtranscript監視に劣ると判断し、**主方式としてはtranscript(jsonl)監視を採用する。**

## スコープ（What / Not What）

### 含むもの（What）

- Windows常駐（バックグラウンド/タスクトレイ）アプリとして、`~/.claude/projects/**/*.jsonl` を横断監視し、Claude Code CLIのセッション（5時間）レート制限のヒットを検知する
- ヒット検知時に取得した「resetsテキスト」からリセット時刻を算出し、そのタイミングでWindows OS通知（Toast）を送る
- 複数プロジェクト（複数の作業フォルダ）でClaude Codeを同時に使っていても検知できるよう、プロジェクトディレクトリを横断して監視する
- 週次（weekly）レート制限についても、フェーズAでの技術検証結果次第で同様に対応する（未確認のため条件付き）
- Windows起動時の自動起動（スタートアップ登録）
- GitHub等でのOSS公開、GitHub Sponsors等への投げ銭導線の設置

### 含まないもの（Not What）

- Anthropic API単体の利用量・残高監視（CONCEPT.mdで明記済みの別ユースケース）
- Claude Code以外のツール（Codex, Cursor, Gemini CLI等）の横断監視（Claude特化の方針、CONCEPT.mdで確定）
- レート制限解除後に自動でプロンプトを送信する等の「自動再開」機能。通知のみに留める（自動操作は誤動作リスクがあり、既存OSS(unsnooze/claude-auto-retry等)と機能が被るため初期スコープ外）
- macOS/Linux対応（Windows専用、CONCEPT.mdで確定）
- マルチアカウント監視・チーム共有・Slack/Discord通知等（CONCEPT.mdの「有料の機能制限は設けない」方針とも整合し、実装は将来検討に留める）
- リアルタイムの残量%表示・ステータスバー常時表示（コア体験である「上限到達→リセット通知」に集中する）

## 想定ユーザーと主要ユースケース

**想定ユーザー**: Claude Code CLIをWindows上で日常的に使う開発者/パワーユーザーで、セッション上限に頻繁に達し、リセットタイミングの手動管理を面倒に感じている人。

**主要ユースケース**:
1. セッション上限に達して一旦PCから離れる → リセット時刻にWindows通知が届き、作業を再開できる
2. 複数プロジェクトで並行してClaude Codeを使っている人が、どのプロジェクトで上限に達したかを気にせず、アカウント全体のリセットタイミングを一箇所で把握できる
3. （週次上限のフォーマット確認後）週次上限に達した際も同様に通知を受け取れる

## 技術方針

- **実装言語/ランタイム**: 未確定。Windows常駐＋トレイアイコン＋Toast通知＋ファイルシステム監視という要件を満たせることを必須条件とし、具体的な選定（Node.js/Electron系、.NET(C#)、Python(pystray等)のいずれか）は**generatorエージェントの判断に委ねる（要確認事項）**。判断基準として、Windowsネイティブなトースト通知が出せること、単一ファイル/軽量な配布がしやすいこと、保守しやすいことを重視する。
- **通知方式**: Windows Toast通知（Action Center通知）をネイティブAPIで実装する。外部プッシュ通知サービスには依存しない（ローカル完結、プライバシーとシンプルさを優先）。
- **ファイル監視方式**: `~/.claude/projects/**/*.jsonl` をファイルシステムウォッチまたはポーリングで監視し、**追記された新しい行のみを差分解析する**（transcriptファイルは数万行規模になり得るため、tail方式が必須。全体再読み込みは避ける）。
- **対応OS**: Windows専用（CONCEPT.mdで確定）。

## 実装ステップ（機能単位）

### フェーズA: 実装（MVP）

1. **技術検証**: レート制限検知方式を検証する（週次上限メッセージの実例収集、タイムゾーン表記のバリエーション確認を含む）
2. JSONLファイル監視・レート制限検知機能を実装する（`~/.claude/projects/**/*.jsonl`の横断tail監視、合成rate_limitメッセージの検出）
3. リセット時刻パース・通知スケジューリング機能を実装する（resetsテキストの解析、絶対リセット日時の算出、通知タイマー登録）
4. Windows OS通知（Toast）送信機能を実装する
5. 常駐化（タスクトレイアイコン表示・Windows起動時の自動起動登録）を実装する
6. 複数プロジェクト同時監視・エラーハンドリング・ログ整備を行う（フォーマット変更時の警告ログ等、防御的実装を含む）
7. 実機動作確認を行う（実際にレート制限へ到達させた上での検知精度・通知タイミングの検証）
8. **[マイルストーン] MVP実装完了**

### フェーズB: OSS公開

9. README・セットアップドキュメントを整備する
10. 配布形式（インストーラー/ポータブルexe）を作成する
11. ライセンスを選定しGitHubリポジトリを公開する
12. **[マイルストーン] OSS公開完了**

### フェーズC: 認知獲得・投げ銭導線

13. 投げ銭導線（GitHub Sponsors等）を設置する
14. note記事を執筆・公開する（技術記事・使い方チュートリアル、認知獲得と投げ銭導線への流入経路）
15. **[マイルストーン] 認知獲得・投げ銭導線構築完了**

### フェーズD: フィードバック・将来検討

16. 利用状況・フィードバックを収集する
17. マルチアカウント対応・有料プラン等の将来展開を企画検討する（CONCEPT.mdの競合調査で挙げた「本命」戦略の検討。実装確約ではない）

## 公開・収益化パイプライン（一連の流れ）

```
フェーズA(実装/MVP) → フェーズB(README整備→配布形式作成→GitHub公開) → OSS公開完了
  → フェーズC(投げ銭導線設置→note記事公開) → 認知獲得・投げ銭導線構築完了
  → フェーズD(フィードバック収集→将来の有料プラン検討)
```

無料コア機能を先にOSSとして公開し、投げ銭導線とnote記事による認知獲得を組み合わせることで、CONCEPT.mdで確定した「無料＋投げ銭」モデルを具体化する。将来の有料プラン（マルチアカウント対応等）は、CONCEPT.mdの「有料の機能制限は設けない」方針を維持しつつ、あくまで追加価値の提供として企画検討フェーズに位置づける。

## 依存関係・リスク・未決事項

### 依存関係

- Claude Code CLIが `~/.claude/projects/**/*.jsonl` にセッションtranscriptを書き続けるという、現時点の非公式な内部実装に強く依存する。

### リスク

1. **内部フォーマット変更リスク**: `~/.claude/projects/*.jsonl` の形式・文言はAnthropicの公式APIではないため、CLIのバージョンアップで変わりうる。対策としてパターンマッチを疎結合に実装し、フォーマット不一致検知時にログ警告を出す設計とする。
2. **週次上限メッセージ未確認**: フェーズAで実データ確認、または既存OSS（ccusage, code-notify, claude-auto-retry, unsnooze等）のソースコード読解による仕様確認が必要。
3. **タイムゾーン表記のパース**: `Etc/GMT-9` のような符号反転オフセット表記を汎用的にパースする実装が必要（決め打ち禁止）。
4. **複数セッション/同時実行時の挙動未検証**: レート制限ヒットのログが実際にAPIコールが発生したプロジェクトのtranscriptにのみ記録されるか（全プロジェクトへの伝播はないと推測）は未検証。全プロジェクト横断監視で対応するが、監視対象が増えることによるポーリング負荷は留意点。
5. **保守コストの発生**: CLI側の変更に追従するための通知アプリ自体の更新のしやすさ（自動アップデート機構の要否）は初期スコープ外だが、将来検討課題として残る。

### 未決事項（要確認）

1. 実装言語・ランタイムの最終選定（Node.js/Electron vs .NET vs Python）→ generatorエージェントの判断に委ねる。
2. 週次上限メッセージの正確なフォーマット。
3. 通知アプリ自体の配布形式（単体exe/インストーラー/Microsoft Store等）。
4. 複数のClaude Codeアカウント（将来のマルチアカウント対応）をどう扱うか → 初期スコープ外、フェーズD(17)で将来検討。

## 関連ファイル・カード

- コンセプト: `token-recovery-notifier/CONCEPT.md`
- processgraph: `ludqoze`（Claudeのトークン回復通知アプリ、本PLANの実装ステップが所属）、`rtrx6va`（アプリ制作ゴール）
